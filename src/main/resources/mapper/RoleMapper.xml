<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fei.item.mapper.RoleMapper">
    <resultMap id="BaseResultMap" type="cn.fei.item.domain.entity.Role">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <collection property="permissionList" ofType="cn.fei.item.domain.entity.Permission"
                    column="id" select="cn.fei.item.mapper.PermissionMapper.selectByRoleId"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        DELETE FROM role
        WHERE id = #{id,jdbcType=BIGINT}
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="cn.fei.item.domain.entity.Role" useGeneratedKeys="true">
        INSERT INTO role (name)
        VALUES (#{name,jdbcType=VARCHAR})
    </insert>
    <update id="updateByPrimaryKey" parameterType="cn.fei.item.domain.entity.Role">
        UPDATE role
        SET name = #{name,jdbcType=VARCHAR}
        WHERE id = #{id,jdbcType=BIGINT}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
            id,
            name
        FROM role
        WHERE id = #{id,jdbcType=BIGINT}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
            id,
            name
        FROM role
    </select>
    <!--通过用户id获取角色集合-->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
            id,
            name
        FROM role
        WHERE id IN (SELECT role_id
                     FROM user_role
                     WHERE user_id = #{userId})
    </select>
</mapper>